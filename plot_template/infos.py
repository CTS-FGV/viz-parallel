from .get_raw_data import output

raw_data = output['raw_data']


def infos(callback_input: dict) -> dict:
    """
    Receives the callback input and return a dict with infos wanted
    :param callback_input:
    :return:
    """
    pass

output = {'infos': infos}
